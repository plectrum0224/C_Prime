#include <iostream>
#include <string>
#include <vector>
#include "course.h"
using namespace std;

vector<CourseInfo>::size_type courseSize(vector<CourseInfo> vec_courseInfo){
    return vec_courseInfo.size();
}
